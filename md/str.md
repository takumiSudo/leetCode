To get the size (length) of a string in Python, use the built-in `len()` function:

```python
s = "Hello, World!"
size = len(s)
print(size)  # Output: 13
```

This will return the number of characters in the string `s`.